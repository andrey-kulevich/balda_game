import model.Cell;
import model.Dictionary;
import model.Game;
import model.Player;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

import java.io.FileNotFoundException;

public class ComputerPlayerTest {

    // search sequences, min depth

    // search sequences, max depth

    // filter sequences

    // find words in dictionary, one word founded

    // find words in dictionary, several words founded

    // find words in dictionary, reversed word founded

    // find words in dictionary, no words founded

}

