public class PlayerTest {
    // create player, null name

    // set game, null game

    // add word, null word

    // select cell, success

    // cell is not selected !!!!

    // write to cell, success

    // cannot write to cell

    // undo all actions, committed in current move
}
